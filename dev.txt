# In Progress
- Manual QA multi-role dashboards

# Completed (latest)
- [DONE] Fix listing photo on home — Free plans can set hero_image_url;
  public_hero_image_url always uses saved photo; appearance + partner edit
  show Listing photo URL with preview

- [DONE] Inline app.css in <head> (cached) — removes HTML→CSS critical-path
  chain from PageSpeed network dependency tree

- [DONE] Image delivery — Unsplash q=45/fm=webp, srcset max 640w, tighter
  sizes; logo WebP (~10KB) + smaller PNG for header

- [DONE] Tailwind compiled build — static/css/app.css (~44KB minify), removed
  CDN from base + Mission Control; Docker multi-stage node build; npm run build:css

- [DONE] Perf: PageSpeed pass — async Google Fonts, deferred local HTMX,
  critical CSS first paint, Unsplash w=640&q=60 + srcset on cards
  (Tailwind CDN still render-blocking; compile CSS is next)

- [DONE] Sitemap for Google — fast build_sitemap_entries (1 DB pass), cached
  /sitemap.xml, generate_sitemap management command → static/sitemap.xml
  Submit: https://suratbazar.online/sitemap.xml in Search Console

- [DONE] Fix index migrations for prod — CREATE INDEX IF NOT EXISTS via
  SeparateDatabaseAndState (booking/billing/crm/leads/users) so migrate
  succeeds when indexes already exist from a partial apply

- [DONE] Perf pass 2: capped booking calendar_view to latest 200 appointments;
  capped enquiries_for_user to latest 100 (ENQUIRY_INBOX_LIMIT); cached
  get_industry_facet_counts/get_area_facet_counts via existing directory
  cache versioning; capped /surat/<industry>/ and /surat/<industry>/<area>/
  grid render to DIRECTORY_LIST_LIMIT (24) with "Showing X of Y" note,
  stats/JSON-LD still computed off full result set.
- [DONE] Perf: added Meta.indexes — Invoice(business,status,created_at),
  Appointment(business,date)/(business,status)/(business,created_at),
  Enquiry(business,status)/(client,status), Business(industry_type),
  Business(profile_setup_completed), Customer(business,phone). Migrations
  billing.0002, booking.0003, crm.0003, leads.0002, users.0008 — reviewed
  (pure AddIndex, no data ops) and applied.

- [DONE] Reorder home categories — Food, Fashion, Gym, Tuition, Optical first

- [DONE] Compact home — DIRECTORY_LIST_LIMIT=24, slim hero (no mosaic/featured),
  tighter categories/areas, “Showing X of Y” when capped

- [DONE] Hero section upgrade — two-column layout, location badge, inline stats pills,
  quick HTMX filters, prominent search + mosaic of featured listings (desktop)

- [DONE] Home page upgrade — KPI stats strip, category icon grid (HTMX filter),
  popular areas chips, featured listings carousel, improved search card + hero,
  business-owner CTA, industry hub links; directory results polish

- [DONE] Bulk seed command seed_bulk_directory — 200 realistic Surat listings
  (areas, industries, Free/Pro/Premium, services, hours, products)

- [DONE] Production ready: .env.prod (suratbazar.online, port 8998, IP 159.195.52.197),
  docker-compose bind 127.0.0.1:8998, depoly.txt Nginx+SSL notes

- [DONE] Perf: cached list_directory_businesses() (config.CACHES, LocMemCache
  by default, REDIS_URL env to switch backend). Versioned cache key via
  bust_directory_cache(), called from business profile/plan/services/hours
  writes. DIRECTORY_CACHE_TTL setting (default 60s).
- [DONE] Perf: fixed N+1s — booking calendar_view select_related, partners
  admin list annotate+prefetch, businesses admin list count()->|length.
  Deduped get_similar_businesses from re-running the full directory query.
- [DONE] Bug fix: billing/checkout_modal no longer resyncs invoice totals to
  current service price once invoice.status == 'paid' (GET-triggered price
  overwrite on a paid invoice). Added regression test.

- [DONE] Brand logo (transparent PNG) in header/footer — static/img/logo*.png

- [DONE] Home category SVG icon strip + shared 4-column category footer on public pages

- [DONE] Products + product enquiry on public listings; dashboard Products/Enquiries
  panels visible; Demo Glow upgraded to Pro with sample products; Enquire CTA per product

- [DONE] Programmatic SEO: /surat/ hub, /surat/<industry>/, /surat/<industry>/<area>/ —
  collection meta + ItemList/Breadcrumb JSON-LD, sitemap + robots Allow,
  home + listing internal links

- [DONE] Bug fix: billing/checkout_modal no longer resyncs invoice totals to
  current service price once invoice.status == 'paid' (GET-triggered price
  overwrite on a paid invoice). Added regression test.

- [DONE] SEO: meta/OG/canonical, LocalBusiness JSON-LD, robots.txt, sitemap.xml, noindex on private dashboards
- [DONE] Seeded roles: seed_roles (+ demo + directory) — partners/clients/enquiries/assignments
- [DONE] Multi-role marketplace: platform_role, partner/client/business/super_admin dashboards, enquiries
- [DONE] Applied users.0007 + leads.0001; admin@test.com → super_admin
- [DONE] Applied control.0001_admin_log; promoted admin@test.com to staff+superuser
- [DONE] Mission Control app at /admin/ (dashboard, users, activity, ban/unban, impersonate)
- [DONE] Django default admin kept as backup at /sd/

- [DONE] Expanded Pro/Premium unlocks: map, testimonial, public email, share tools, custom hero, featured-service limits, card styles, hide branding
- [DONE] Applied users.0006 + catalog.0005 (listing_plan, website_url, Product)
- [DONE] Re-seeded directory with Free/Pro/Premium + sample products

# Completed
- Phase 1 initialization
- Scaffolding apps and models
- [DONE] Home page directory + listing UX
- [DONE] Product listings + listing plans + pricing
- [DONE] Mission Control + multi-role auth/dashboards/enquiries
